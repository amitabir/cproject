#ifndef WINDOW_H_
#define WINDOW_H_
#include <SDL.h>
#include "Widget.h"

/* This function receives a Widget pointer and draws it to the screen with the image required at the correct position. */
int windowDraw(Widget *winodw);

#endif
